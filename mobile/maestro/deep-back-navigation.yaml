appId: com.trailcompanion.app
---
# Test deep navigation stack unwinds correctly
- runFlow: shared/launch-dev.yaml
- extendedWaitUntil:
    visible: "Select a Trail"
    timeout: 30000

# Navigate deep: trail list → overview → map
- tapOn: "View trail →"
- waitForAnimationToEnd
- scrollUntilVisible:
    element:
      text: "Open Map"
    direction: DOWN
    timeout: 10000

- tapOn: "Open Map"
- waitForAnimationToEnd

# Unwind: map → overview
- pressKey: back
- waitForAnimationToEnd
- extendedWaitUntil:
    visible: "Australian Alps Walking Track"
    timeout: 10000

# Unwind: overview → trail list
- pressKey: back
- waitForAnimationToEnd
- extendedWaitUntil:
    visible: "Select a Trail"
    timeout: 10000
