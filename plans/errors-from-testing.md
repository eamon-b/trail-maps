# Errors from testing

## Resolved / obsolete

- ~~"show on elevation profile does not in fact bring up an elevation profile"~~ —
  **obsolete (2026-08-07).** This referred to the pre-rebuild "Trail Companion" app
  (retired by the `rebuild/farout-clone` teardown). The Tracknotes guide shell has no
  "show on elevation profile" action; the Elevation pane is a first-class toggle in the
  guide shell and was verified working on the emulator (waypoints plotted, tap-to-read-off,
  pinch-zoom, GPS marker).

## Open (from the 2026-08-07 emulator walkthrough)

Tracked in `plans/tracknotes-backlog.md` and the repo TODO — see the walkthrough summary
for details: Larapinta data fixes (Sonder spur / duplicate waypoint IDs / direction label),
composer registration error handling, settings display-name rename, home-screen settings
entry point.
