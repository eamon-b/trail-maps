# Tracknotes Backlog

Living list of known follow-ups for the FarOut-clone rebuild (merged to `main` 2026-08-18).
Master plan: `~/.claude/plans/i-want-to-start-melodic-reddy.md`. Update this file as items land or emerge.

## Required before any App Store / Play Store release
- [x] **Report/flag endpoint + UI** — done 2026-08-19: `POST /v1/comments/:id/report` (idempotent per device+comment, 20/24h rate limit, reasons spam/offensive/inaccurate/other + optional detail) with `GET /v1/admin/reports` for moderation, `comment_reports` table in migration 0003. Mobile: Report action on non-own comments → reason-picker modal (`src/features/comments/ReportDialog.tsx`), offline-first via outbox `kind='report'`; unregistered devices get the same display-name prompt as first post.
- [x] **Account deletion** — done 2026-08-19: `DELETE /v1/me` soft-deletes all the user's comments (tombstones sync out), deletes their R2 photos, scrubs + locks the user row (comments inner-join users, so the row is anonymized rather than hard-deleted). Mobile: Settings → Delete account (`src/features/settings/DeleteAccountSection.tsx`) with confirm modal; clears session, purges local authored comments + outbox; network failure keeps the account intact.
- [ ] **Privacy policy + D1 data-residency check** — draft policy now at `docs/privacy-policy.md` and the residency-verification procedure + APP posture at `docs/data-residency.md` (policy deliberately does NOT claim AU residency — hints are best-effort). Remaining before release: run the `wrangler d1 info` / `r2 bucket info` checks with cloud auth, log results in `docs/data-residency.md`, publish the policy to a public URL and link it from the store listings + Settings.

## Product features (planned phases)
- [x] **Phase 6 — check-in sharing (share-sheet half)**: shipped — `src/features/share/{check-in.ts,use-check-in-share.ts,ShareIconButton.tsx}`. Backend check-ins remain a follow-up.
- [x] **Phase 7 — custom route builder**: shipped — `src/features/routes/`, `src/db/routes-repo.ts`, `app/guide/[trailId]/routes.tsx` (route drawing on the map pane, saved-routes screen).
- [x] **Phase 8 — planner heritage**: shipped — live plan calculator at `app/guide/[trailId]/plan.tsx` over `src/features/plan/` + `@lib` calculators (see `PLAN-hours-based-planning.md`, incl. post-review fixes).
- [x] **Aggregated water-status chip** — done 2026-08-19: freshness ranking (`exp(-age_days/30)`, 120-day window) in `src/features/guide/water-aggregate.ts` over cached reports (`src/db/water-status-repo.ts`), surfaced as a chip on water waypoints in the list pane (`useWaterStatus` hook) and as status-colored waypoint rings on the map (data-driven `match` on a new `waterStatus` feature property). Own offline reports count immediately; chip refreshes on sync events.
- [x] **Display-name rename UI** — done: Settings → Account shows the current display name with an inline editor (`src/features/settings/DisplayNameSection.tsx`) calling `identity-store.rename` → `PATCH /v1/me`; client-side trim/length check mirrors the server's 40-char limit, network failures render inline and keep the previous name. Anonymous devices get an explanatory row instead (registration still happens on first post, never from Settings).
- [x] **Waypoint descriptions enrichment (mechanism)** — done 2026-08-19: `waypoint_descriptions` table + public `GET /v1/trails/:trailId/descriptions?since=` + admin `PUT /v1/admin/trails/:trailId/descriptions/:waypointId` on the worker; mobile pulls deltas alongside comment sync into a `waypoint_meta` table (schema v3) and the detail screen renders `synced ?? bundled`. **Content authoring remains**: curated descriptions still need to be written and PUT via the admin endpoint.

## Design divergences awaiting Eamon's review
- [ ] **Header glyph vs overflow menu (Phases 7 + 8)** — the master plan specifies an *overflow-menu "Plan" entry*; instead both Phase 7 (Routes ⋔) and Phase 8 (Plan ▤) shipped as always-visible inline header glyphs, making four header actions (⋔ ▤ ⤓ ⚙) beside the guide title. Consistent with each other but divergent from the plan, and React Navigation truncates the *title*, not the buttons, so long guide names (e.g. "Hume & Hovell") get squeezed on narrow screens. Decide: keep the four glyphs, or fold Routes + Plan (and maybe Downloads) into a single ⋯ overflow menu. _(Not code-closeable — needs Eamon's call.)_

## Verification gaps (need a human + device — not closeable from a coding session)
- [ ] **Offline tile download E2E** — untested end-to-end. Add `EXPO_PUBLIC_TILE_BASE_URL` to `mobile/.env.local`, download Cape to Cape, verify the `mbtiles://` offline style renders in airplane mode (the map remount on style-source flip is tested in Jest only).
- [ ] **Dark theme visual QA** — themes are WCAG-checked arithmetically but never eyeballed on device.
- [ ] **iOS smoke test** — all E2E tooling is Android; run an EAS iOS build and hand-check before any TestFlight.
- [ ] **Run the new Maestro flows** — the five ported flows (see Tech debt below) are authored against current UI text but unverified: no emulator was available when they were written. Run each against the local emulator + Metro and fix selector drift.

## Data quality
- [x] **Duplicate waypoint IDs in larapinta.json** — RESOLVED 2026-08-07. Not a registry-matching bug: the Mt Sonder summit out-and-back was folded into the single "Main Route" trkseg, so `enrichWaypoints` (one row per proximity episode) matched the three Redbank waypoints twice and fanned one source waypoint into two rows sharing an id. Fixed by lifting the spur into a side trip via new opt-in `extractSpurs` config (`src/lib/track-spurs.ts`), reversing the Larapinta track so km 0 is Alice Springs (new `reverseTrack` config — the existing "Westbound" label was inverted), and adding a duplicate-id build invariant in `processTrail` so the fan-out can never ship again. Larapinta is now 215.8 km with 58 unique on-trail waypoints; `data/waypoint-ids.json` unchanged (ids are minted from lat/lon, so reversal is a no-op for them).
- [x] **Near-duplicate waypoint dedupe** — done 2026-08-19: new pure module `src/lib/waypoint-dedupe.ts` (same name + same type + ≤150 m → single-linkage merge; survivor = smallest km along track; descriptions merged; dropped ids kept as `mergedIds`), wired into `scripts/build-trails.ts` after ID assignment so `data/waypoint-ids.json` stays byte-identical (verified zero diff). Opt-in `dedupeWaypoints` config enabled for AAWT (14 merges incl. Talbot Hut Site, across main/variant/off-trail views) and Larapinta (11 merges incl. Rocky Bar Gap, Rocky Gully). Mobile assets regenerated. Note: comments filed against a retired id stay server-side but won't show on the survivor's screen — acceptable pre-release; revisit if real comments ever exist on a merged-away id.

## Tech debt / infra
- [x] **Photo-upload idempotency server-side** — done 2026-08-19: worker hashes uploaded bytes (sha-256), stores `photo_hashes_json` index-aligned with `photo_urls_json` (migration 0003); a replay of already-stored bytes short-circuits to 200 with the existing URL, no R2 write, and succeeds even at the 4-photo cap. Legacy rows (NULL hashes) are padded and simply not replay-protected.
- [ ] **MapLibre RN + Expo SDK upgrade** — pinned at the proven 10.4.x / SDK 54 combo; upgrade deliberately as its own task. _(Intentionally left out of the gap-closing PR.)_
- [ ] **Custom domain for workers** — `*.workers.dev` URLs are baked into shipped builds (same trade-off as the contour worker); a custom domain decouples them. _(Needs a domain + Cloudflare DNS action, not code.)_
- [x] **Comment-feed pagination UI** — done 2026-08-19: detail screen renders the newest 20 with "Show earlier comments (N remaining)" (+20 per tap) over a `LIMIT`-ed repo query; local posts widen the window so nothing drops out.
- [x] **Maestro coverage (flows authored)** — done 2026-08-19: `guide-list`, `view-map`, `toggle-views`, `waypoint-detail`, `add-comment-offline` in `mobile/maestro/` + a README with run instructions and per-flow verification status. All selectors are text/a11y-label based (no testIDs exist in the app). **Unverified on device** — see the Verification gaps entry above. Note discovered while authoring: a fresh device cannot queue an offline comment because first-time registration (`POST /v1/devices`) needs the network; the flow asserts both legal outcomes.
