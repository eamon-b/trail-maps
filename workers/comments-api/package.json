{
  "name": "farout-comments",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "vitest run",
    "test:watch": "vitest",
    "migrate:local": "wrangler d1 migrations apply farout-comments --local",
    "migrate:remote": "wrangler d1 migrations apply farout-comments --remote"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.19.0",
    "@cloudflare/workers-types": "^5.20260722.1",
    "typescript": "^5.0.0",
    "vitest": "^4.1.0",
    "wrangler": "^4.0.0"
  }
}
